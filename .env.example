# ==============================================
# ACCESSIBILITY MONITOR TOOL - Environment Variables
# ==============================================
# Copy this file to .env and fill with your values
# 
# NEVER commit .env with real credentials to git!
# Use GitHub Secrets for production/CI environments
# ==============================================

# Emergency Contact Configuration
# --------------------------------
# Email to receive critical accessibility alerts
EMERGENCY_EMAIL=your-emergency-email@company.com

# Phone number for emergency contacts
EMERGENCY_PHONE=+351900000000

# Authority email for legal compliance notifications
AUTHORITY_EMAIL=authority@company.com

# SMTP Email Configuration
# ------------------------
# SMTP server hostname (Gmail: smtp.gmail.com)
SMTP_HOST=smtp.gmail.com

# SMTP server port (Gmail: 587)
SMTP_PORT=587

# Email address used to send notifications
SMTP_USER=your-smtp-user@company.com

# SMTP password or app-specific password
# For Gmail: Generate app password in Google Account settings
SMTP_PASS=your-app-password-here

# Optional: Enable/disable email notifications
# Set to 'false' to disable all email functionality
EMAIL_ENABLED=true

# Optional: Notification thresholds
# Minimum score to trigger emergency alerts
EMERGENCY_SCORE_THRESHOLD=70

# Minimum critical violations to trigger alerts
EMERGENCY_CRITICAL_THRESHOLD=3

# Development/Testing Configuration
# ---------------------------------
# Set to 'development' for local testing
# Set to 'production' for live environment
NODE_ENV=development

# Enable debug logging (true/false)
DEBUG_MODE=false

# Browser timeout in milliseconds
BROWSER_TIMEOUT=60000

# ==============================================
# SETUP INSTRUCTIONS:
# ==============================================
# 
# 1. Local Development:
#    - Copy this file: cp .env.example .env
#    - Fill in your values in .env
#    - Test: yarn audit:portfolio console
#
# 2. Production/CI (GitHub):
#    - Go to: Repository > Settings > Secrets and variables > Actions
#    - Add each variable as a separate secret
#    - Use exact names (EMERGENCY_EMAIL, SMTP_HOST, etc.)
#
# 3. Verify setup:
#    - yarn emergency --validate
#
# ==============================================

# Periodic Monitoring Configuration
# Cron expression for periodic monitoring (default: weekly on Monday at midnight)
MONITORING_INTERVAL=0 0 * * 1
